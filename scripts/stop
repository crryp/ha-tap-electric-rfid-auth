#!/usr/bin/env bash
# Stop the dev containers. config/ (and its onboarding state) stays on disk.
set -euo pipefail
cd "$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

docker compose -f .devcontainer/docker-compose.yml stop
